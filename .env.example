# AYNI Marketplace environment configuration
# Copy to .env.local and adjust values

# Use mock API (set to "true" for development without backend)
VITE_USE_MOCKS=true

# Backend API base URL (used when VITE_USE_MOCKS=false)
VITE_API_URL=https://api.ayni.gold
